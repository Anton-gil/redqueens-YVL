{
  "v1": {
    "version": "v1",
    "basis_label": "VACUOUS_FOR_RATE_DELTAS",
    "threshold_bps": 1590,
    "reason": "v1's ExchangeRateDeltaBound was 'validated' against benign exchange-RATE deltas. The corpus's own coverage note says the benign rate never moves off 1e18 under proportional deposits, so '0 violations' is true BY CONSTRUCTION and the 15.9% threshold is 10x a 1.59% dust-rounding delta artifact. This is not meaningful false-positive evidence.",
    "accepted": false
  },
  "v2": {
    "version": "v2",
    "basis_label": "measured_conversion_ratio_on_EVM",
    "metric": "credited_principal / (tokensIn * raw_usdFeed.latestPrice())",
    "evm_benign_max_deviation_bps": 0,
    "evm_applicable_deposits_checked": 40,
    "evm_false_positives": 0,
    "evm_evidence_test": "test/ValidationV2.t.sol::testBenignSequenceNoFalsePositive",
    "corpus_sim_max_deviation_bps": 1240,
    "corpus_sim_p99_bps": 1176,
    "corpus_sim_note": "The Python corpus simulation reports up to 1240 bps (p99 1176) of conversion-ratio drift from its COARSE share-rounding model. This is NOT reproduced on the EVM (1e18 integer math), which resolves the F6 contradiction ('never moves off 1e18' vs '1.59% dust'): both describe the simulator's rounding, not EVM behavior. The deployed guard runs on the EVM, so the EVM measurement (0 bps) governs.",
    "tolerance_bps": 100,
    "threshold_basis": "EVM benign deviation is 0 bps; 100 bps is a small ABSOLUTE margin over EVM integer rounding (not a fit to the sim artifact, not the 1% arbitrary floor). The root-cause valuation fix (credit tokensIn * raw unit price directly) removes the pool-rate dependence and would allow ~0 tolerance.",
    "accepted": true
  },
  "ts": 1789850735.4693282
}